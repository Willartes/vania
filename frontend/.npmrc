# Configuração npm — Vania Herculano
# Evita que "npm audit fix --force" atualize versões maiores

# Usar legacy peer deps para compatibilidade Angular 17
legacy-peer-deps=true

# Não atualizar automaticamente para versões incompatíveis
save-exact=true
